
# artefacts de build extension (peuvent contenir des identifiants pre-remplis : ne jamais committer)
proxy-selectif-oxylabs-*.zip
proxy-oxylabs-*.zip
*.zip

# secrets bureau a distance
remote-desktop/.env

# comptes bureau a distance (secrets)
remote-desktop/users.json
