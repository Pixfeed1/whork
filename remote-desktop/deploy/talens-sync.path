[Unit]
Description=Declenche la synchro des postes quand les comptes changent

[Path]
# Quand l'interface admin modifie config.json, on relance la synchro.
PathChanged=/root/whork/remote-desktop/config.json
Unit=talens-sync.service

[Install]
WantedBy=multi-user.target
